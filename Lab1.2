using System;
namespace Lab1_2
{
    public class Program
    {
        static public int Example(int d)
        {
            int d1 = (d - (d % 100)) / 100;
            Console.WriteLine("перша цифра - " + d1.ToString());
            return d1;
        }
        static public int Lastnumber(int d)
        {
            int d2 = d % 10;
            Console.WriteLine("остання цифра - " + d2.ToString());
            return d2;
        }
        static void Main(string[] args)
        {
            Console.WriteLine("Введiть d");
            string S1 = Console.ReadLine();
            int d = int.Parse(S1);
            if (d > 999 || d < 100)
            {
                Console.WriteLine("введене число не є трехзначного"); 
            }
            else
            {
            Example(d);
            Lastnumber(d);
            Console.ReadLine();
            }
        }
    }
}
